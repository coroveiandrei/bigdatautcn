{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
    "cosmosServers": {
        "value": [{
            "name": "[REPLACE_WITH_STORAGE_NAME]",  //just lowercase, just letters
            "rg": "[REPLACE_WITH_RESOURCE_GROUP_NAME]"//just lowercase, just letters
        }]
    }
    }
}
